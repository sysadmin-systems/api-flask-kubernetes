# api-flask-kubernetes
API desenvolvida em Flask para exibir metadados provenientes do Kubernetes


Run the server from the "web" directory:

(env)$ export FLASK_APP=project/__init__.py
(env)$ python manage.py run